<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunshine.mybatis.EmailMapper">
   <!-- <select id="selectEmail" parameterType="java.lang.Integer" resultType="com.sunshine.mybatis.Email">
        select * from email where id = #{id}
    </select>

    <select id="selectEmail" resultType="com.sunshine.mybatis.Email">
        select * from email limit 3
    </select>-->
</mapper>
